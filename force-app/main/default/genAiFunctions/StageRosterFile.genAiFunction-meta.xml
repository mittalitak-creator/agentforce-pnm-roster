<?xml version="1.0" encoding="UTF-8"?>
<GenAiFunction xmlns="http://soap.sforce.com/2006/04/metadata">
    <description>Diffs the validated roster file rows against current provider network records (using NPI as key) and triggers the CRF Stage File action to create the Target DLO. Returns new/updated/no-change counts and provider details for UI-1 confirmation. Only call after admin confirms proceeding with valid rows.</description>
    <developerName>StageRosterFile</developerName>
    <label>Stage Roster File</label>
    <functionType>InvocableAction</functionType>
    <invocableActionName>RosterStagingAction</invocableActionName>
    <invocableActionType>apex</invocableActionType>
</GenAiFunction>
