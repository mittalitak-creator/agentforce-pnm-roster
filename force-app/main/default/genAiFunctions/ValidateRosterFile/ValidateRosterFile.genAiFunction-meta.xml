<?xml version="1.0" encoding="UTF-8"?>
<GenAiFunction xmlns="http://soap.sforce.com/2006/04/metadata">
    <description>Validates the roster CSV file attached to the CRF record. Pass only the CRF record ID — the action fetches and reads the file itself. Returns pass/fail for all 35+ rules, row-level error details, an agentNarration summary, and a validatedRowsJson string to pass directly to StageRosterFile. Run before staging.</description>
    <developerName>ValidateRosterFile</developerName>
    <invocationTarget>RosterValidationAction</invocationTarget>
    <invocationTargetType>apex</invocationTargetType>
    <isConfirmationRequired>false</isConfirmationRequired>
    <isIncludeInProgressIndicator>true</isIncludeInProgressIndicator>
    <masterLabel>Validate Roster File</masterLabel>
    <progressIndicatorMessage>Validating roster file...</progressIndicatorMessage>
</GenAiFunction>
